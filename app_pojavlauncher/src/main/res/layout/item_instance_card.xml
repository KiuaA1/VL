<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/instance_card_root"
    android:layout_width="match_parent"
    android:layout_height="72dp"
    android:layout_marginBottom="10dp"
    android:background="@drawable/bg_instance_card"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="12dp"
    android:paddingEnd="10dp"
    android:elevation="2dp"
    android:clickable="true"
    android:focusable="true">

    <!-- Favorite star -->
    <TextView
        android:id="@+id/instance_favorite"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:gravity="center"
        android:text="★"
        android:textColor="@color/muted_text"
        android:textSize="16sp"
        android:visibility="gone"/>

    <ImageView
        android:id="@+id/instance_image"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginStart="4dp"
        android:background="@drawable/bg_circle_dark"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_px_java_run"
        android:contentDescription="Instance icon"/>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="12dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/instance_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Instance"
            android:textColor="@color/primary_text"
            android:textSize="15sp"
            android:textStyle="bold"/>

        <TextView
            android:id="@+id/instance_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Ready to play"
            android:textColor="@color/secondary_text"
            android:textSize="12sp"/>
    </LinearLayout>

    <!-- Play (center emphasis) -->
    <ImageButton
        android:id="@+id/instance_play"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="@drawable/bg_vl_button_primary"
        android:contentDescription="Play"
        android:padding="12dp"
        android:scaleType="centerInside"
        android:src="@drawable/ic_px_java_run"
        app:tint="#FFFFFF"/>

    <!-- More -->
    <ImageButton
        android:id="@+id/instance_more"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="4dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:contentDescription="More"
        android:padding="8dp"
        android:src="@drawable/ic_more_horiz"
        app:tint="@color/secondary_text"/>
</LinearLayout>
